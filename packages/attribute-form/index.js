import AttributeForm from '../maintain-table/src/attribute-form.vue'

/* istanbul ignore next */
AttributeForm.install = function (Vue) {
  Vue.component(AttributeForm.name, AttributeForm)
}

export default AttributeForm
