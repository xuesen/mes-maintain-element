import SelectUserOnly from '../employee/src/select-user-only.vue'

/* istanbul ignore next */
SelectUserOnly.install = function (Vue) {
  Vue.component(SelectUserOnly.name, SelectUserOnly)
}

export default SelectUserOnly
